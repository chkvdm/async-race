export enum GarageLimits {
  PAGE_LIMIT = 7,
  RANDOM_CAR_CREATE_COUNT = 100,
}

export enum EngineStatus {
  START = 'started',
  DRIVE = 'drive',
  STOP = 'stopped',
}

export enum CarConstants {
  CAR_SIZE = 50,
  CAR_INITIAL_POSITION_IN_PERCENT = 100,
}
